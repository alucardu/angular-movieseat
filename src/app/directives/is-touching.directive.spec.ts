/* tslint:disable:no-unused-variable */

import { IsTouchingDirective } from './is-touching.directive';

describe('Directive: IsTouching', () => {
  it('should create an instance', () => {
    const directive = new IsTouchingDirective();
    expect(directive).toBeTruthy();
  });
});
